#!/usr/bin/env ruby

require 'rubygems'
require 'rqtrotate'

include RQTRotate

if ARGV.length < 2
  puts "Usage: qtrotate_set [path] [degrees]"
else
  File.open(ARGV[0], File::RDWR) do |datastream|
    rotate datastream, ARGV[1].to_i
  end
end
