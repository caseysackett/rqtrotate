#!/usr/bin/env ruby

require 'rubygems'
require 'rqtrotate

include RQTRotate

if ARGV.length == 0
  puts "Usage: qtrotate_get [path]"
else
  File.open(ARGV[0], File::RDWR) do |datastream|
    puts get_rotation ARGV[0]
  end
end
